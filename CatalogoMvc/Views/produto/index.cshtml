@{
    ViewData["Title"] = "Catálogo de Produtos";
    var produto = ViewBag.Produtos as List<string> ?? new List<string>();
    var precos = ViewBag.Precos as List<double> ?? new List<double>();
}

<h2>Catálogo de Produtos</h2>

<table >
    <thead>
        <tr>
            <th>#</th>
            <th>Nome</th>
            <th>Preço</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < ViewBag.produto.Count; i++)
        {
            <tr>
                <td>@(i + 1)</td>
                <td>@ViewBag.produto[i]</td>
                <td>@ViewBag.preco[i].ToString("C")</td>
            </tr>
        }
    </tbody>
</table>